# Run Tomcat as this user ID. Not setting this or leaving it blank will use the
# default of tomcat6.
TOMCAT6_USER=tomcat6

# Run Tomcat as this group ID. Not setting this or leaving it blank will use
# the default of tomcat6.
TOMCAT6_GROUP=tomcat6

# The home directory of the Java development kit (JDK). You need at least
# JDK version 1.5. If JAVA_HOME is not set, some common directories f
# OpenJDK, the Sun JDK, and various J2SE 1.5 versions are tried.
#JAVA_HOME=/usr/lib/jvm/openjdk-6-jdk
JAVA_HOME=/usr/lib/jvm/java-7-oracle

# You may pass JVM startup parameters to Java here. If unset, the def
# options will be: -Djava.awt.headless=true -Xmx128m -XX:+UseConcMark
#
# Use "-XX:+UseConcMarkSweepGC" to enable the CMS garbage collector (
# response time). If you use that option and you run Tomcat on a mach
# exactly one CPU chip that contains one or two cores, you should als
# the "-XX:+CMSIncrementalMode" option.
JAVA_OPTS="-Xms160g -Xmx160g -XX:+UseConcMarkSweepGC -XX:NewRatio=3"
JAVA_OPTS="${JAVA_OPTS} -Djavax.sql.DataSource.Factory=org.apache.com
JAVA_OPTS="${JAVA_OPTS} -Dcom.sun.management.jmxremote.port=1099 -Dco
JAVA_OPTS="${JAVA_OPTS} -Djava.rmi.server.hostname=172.21.182.118"
JAVA_OPTS="${JAVA_OPTS} -Dcom.sun.management.jmxremote.password.file=
JAVA_OPTS="${JAVA_OPTS} -Dcom.sun.management.jmxremote.access.file=/e

# To enable remote debugging uncomment the following line.
# You will then be able to use a java debugger on port 8000.
#JAVA_OPTS="${JAVA_OPTS} -Xdebug -Xrunjdwp:transport=dt_socket,addres

# Java compiler to use for translating JavaServer Pages (JSPs). You c
# compilers that are accepted by Ant's build.compiler property.
#JSP_COMPILER=javac

# Use the Java security manager? (yes/no, default: no)
#TOMCAT6_SECURITY=no

# Number of days to keep logfiles in /var/log/tomcat6. Default is 14
#LOGFILE_DAYS=14

# Location of the JVM temporary directory
# WARNING: This directory will be destroyed and recreated at every st
#JVM_TMP=/tmp/tomcat6-temp

# If you run Tomcat on port numbers that are all higher than 1023, th
# do not need authbind.  It is used for binding Tomcat to lower port
# NOTE: authbind works only with IPv4.  Do not enable it when using I
# (yes/no, default: no)
#AUTHBIND=no

# Unset LC_ALL to prevent user environment executing the init script
# influencing servlet behavior.  See Debian bug #645221
unset LC_ALL
